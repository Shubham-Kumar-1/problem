def icecreamParlor(m, arr):
    # Write your code here
    for x in range(len(arr)):
        for y in range(len(arr)):
            if x != y:
                if arr[x] + arr[y] == m:
                    return (x + 1), (y + 1)
