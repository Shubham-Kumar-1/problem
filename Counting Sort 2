def countingSort(arr):
    # Write your code here
    freq=[0]*100
    s_arr=[]
    for i in arr:
        freq[i]+=1
    
    for i in range(len(freq)):
        num=[i]*freq[i]
        s_arr.extend(num)
        
    return s_arr
